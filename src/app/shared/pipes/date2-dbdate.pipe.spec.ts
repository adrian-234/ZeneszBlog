import { Date2DBdatePipe } from './date2-dbdate.pipe';

describe('Date2DBdatePipe', () => {
  it('create an instance', () => {
    const pipe = new Date2DBdatePipe();
    expect(pipe).toBeTruthy();
  });
});
